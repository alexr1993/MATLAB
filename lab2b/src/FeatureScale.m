function [ vector ] = FeatureScale( vector )
%FEATURESCALE Scales and mean normalises feature vector
%   Detailed explanation goes here
    %m = mean(vector);
    %sd = std(vector);
    %vector = (vector - m) ./ sd;
end

